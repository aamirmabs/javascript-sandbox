<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Random</title>
  </head>
  <body>
    <h1>
      Random Person name: <%= person.name %> Age: <%= person.age %> Gender: <%
      if (person.gender === 2) { %> Male <% } %> <% if (person.gender === 1) {
      %> Female <% } %>
    </h1>
  </body>
</html>
